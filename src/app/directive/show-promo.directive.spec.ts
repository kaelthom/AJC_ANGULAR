import { ShowPromoDirective } from './show-promo.directive';

describe('ShowPromoDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowPromoDirective();
    expect(directive).toBeTruthy();
  });
});
